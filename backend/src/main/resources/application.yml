server:
  port: ${PORT:8080}

spring:
  config:
    import:
      - optional:file:../.env[.properties]
      - optional:file:.env[.properties]

  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:zipdaum}?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: always

  # ===== Redis 설정 추가 =====
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

  # ===== 메일 설정 추가 =====
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME} # .env 파일에서 주입
    password: ${MAIL_PASSWORD} # .env 파일에서 주입
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

mybatis:
  mapper-locations: classpath:/mappers/**/*.xml

property:
  real-estate:
    service-key: ${PUBLIC_DATA_SERVICE_KEY:}
    connect-timeout: ${PUBLIC_DATA_CONNECT_TIMEOUT:5s}
    read-timeout: ${PUBLIC_DATA_READ_TIMEOUT:30s}
  kakao:
    rest-api-key: ${KAKAO_REST_API_KEY:}
  ai:
    gms-url: ${GMS_OPENAI_URL:https://gms.ssafy.io/gmsapi/api.openai.com/v1/chat/completions}
    model: ${GMS_OPENAI_MODEL:gpt-5.5}
    api-key: ${GMS_KEY:}
    connect-timeout: ${GMS_CONNECT_TIMEOUT:5s}
    read-timeout: ${GMS_READ_TIMEOUT:180s}

jwt:
  secret: ${JWT_SECRET}
  access-token-expiration: ${JWT_ACCESS_TOKEN_EXPIRATION:3600000}

app:
  cors:
    allowed-origins: ${FRONTEND_ORIGINS:http://localhost:5173,http://127.0.0.1:5173,https://zipdaum.vercel.app}
    allowed-origin-patterns: ${FRONTEND_ORIGIN_PATTERNS:https://*.vercel.app}
